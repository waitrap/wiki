### **Python相关**
1.在使用pip安装flask时，总是失败报错，查看pip版本较低，对其进行升级后，重新使用pip安装可以解决问题。升级办法是首先使用命令`wget https://bootstrap.pypa.io/pip/3.4/get-pip.py`(版本要与python版本对应)获取到对应的`get-pip.py`文件，之后使用`python3 get-pip.py`运行即可。pip相关的文档链接如下:[pip](https://pip.pypa.io/en/stable/)